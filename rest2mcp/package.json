{
  "name": "rest2mcp_svelte",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler pages deploy dist/ --project-name=rest2mcp",
    "deploy:dry-run": "npm run build && wrangler pages deploy dist/ --project-name=rest2mcp --dry-run"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.1.1",
    "svelte": "^5.0.0",
    "vite": "^6.4.3",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.106.0"
  }
}
